services:
  - type: web
    name: booking-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app.serve
    healthCheckPath: /health
    autoDeploy: true
